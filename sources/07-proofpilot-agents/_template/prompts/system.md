# System prompt

Replace this file with your agent's system prompt.

Use plain Markdown — the `.md` format renders cleanly in GitHub diffs,
pastes into the Anthropic Workbench without escaping, and avoids the
Python multi-line-string quoting traps.
