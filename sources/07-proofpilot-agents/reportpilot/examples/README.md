# ReportPilot examples

Drop sanitized example reports here so future runs can learn what
"good" looks like — especially reports that correctly disclosed data
gaps, stayed concentrated on 2–4 strong insights, and avoided
template residue.

File naming: `YYYY-MM-<client-slug>-<report_type>.md`.
