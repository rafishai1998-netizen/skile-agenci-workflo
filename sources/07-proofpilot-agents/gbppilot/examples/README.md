# GBPPilot examples

Drop shipped strategy documents here (sanitized) so future runs can
learn from past decisions — especially category choices, mirroring
matrices, and rollout plans that turned out to be correct.

File naming: `YYYY-MM-DD-<client-slug>-gbp-strategy.md`.
