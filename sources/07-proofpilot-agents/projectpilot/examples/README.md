# ProjectPilot examples

Drop sanitized example sweep outputs here so future runs can learn
what "good" looks like — especially findings that correctly avoided
re-flagging already-moved work, and digests that changed behavior.

File naming: `YYYY-MM-DD-<sweep_type>.md`.
