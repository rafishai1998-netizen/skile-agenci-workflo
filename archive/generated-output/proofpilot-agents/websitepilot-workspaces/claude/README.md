# Claude WebsitePilot Workspaces

Claude Code / Gemini-authored WebsitePilot runs live here so they do not
get mixed with Codex outputs.

Use one folder per client slug:

```text
websitepilot/workspaces/claude/<client-slug>/
```

Public preview URLs must not include `claude` or `gemini`; use the client
slug only.
