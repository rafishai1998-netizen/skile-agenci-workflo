# Codex WebsitePilot Workspaces

Codex-owned WebsitePilot runs live here so they do not get mixed with
Claude Code / Gemini outputs.

Use one folder per client slug:

```text
websitepilot/workspaces/codex/<client-slug>/
```

Public preview URLs must not include `codex`; use the client slug only.
